@* Generator: MvcHelper *@
@using BudgetOnline.UI.Models.ViewCommands
@functions {
}
@helper Render(IEnumerable<ViewCommandUIModelHelper> commands)
    {
        if (commands == null || !commands.Any())
        {
        }
        else
        { 
            foreach (var command in commands)
            {
    @_Views_ListViewCommands_ViewCommandUI_cshtml.Render(command.GetResult(), true)
            }
        }
}